# python-jwt-example-api
Example Restful API with supporting JWT by Flask Python

How to Run:
```
cd python-jwt-example-api
VENV_PATH="./"
python3 -m venv $VENV_PATH
$VENV_PATH/bin/pip install -r requirements.txt
$VENV_PATH/bin/python app.py
```
